import { FC } from "react";
import { LoginForm } from "../components/LoginForm";

export const Login: FC = () => {
    return (
        <div className="Login">
            <LoginForm />
        </div>
    );
}